# Passport Login Example (Noncompliant)

This example shows an Express server using Passport.js for local authentication.

⚠️ The `/login` route **does not** regenerate the session after login, which is a security best practice.

## Usage

```bash
npm install
npm start
